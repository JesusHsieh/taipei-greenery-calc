// ── Shared UI tokens (Stitch "Clinical Architect" design system) ──────────────

/** Section card: no border, tonal background lift */
export const cardCls = 'bg-surface-container-lowest rounded-xl p-6 mb-6 shadow-[0_2px_12px_rgba(43,52,55,0.06)]';

/** Section heading */
export const hCls = 'font-headline text-lg font-bold text-[#0D4D4D] mb-5 flex items-center gap-2';

/** Standard labeled input */
export const labelCls = 'block text-xs font-semibold text-on-surface-variant mb-3 uppercase tracking-wider';

/** Input field — bottom-accent style */
export const inputCls = 'input-field';

/** Sub-section container (tonal low background) */
export const subCardCls = 'bg-surface-container-low rounded-xl p-5 mb-4';

/** Metric chip label */
export const metaLabel = 'text-xs text-on-surface-variant font-medium';

/** Large metric value */
export const metricVal = 'text-2xl font-bold font-headline text-on-surface';

/** Pass/fail pill */
export const passPill  = 'px-3 py-1 rounded-full text-xs font-bold bg-tertiary-container text-on-tertiary-container';
export const failPill  = 'px-3 py-1 rounded-full text-xs font-bold bg-error-container text-on-error-container';
export const pendPill  = 'px-3 py-1 rounded-full text-xs font-bold bg-surface-container-high text-on-surface-variant';
